import styled from "styled-components";

const TitleHeader = styled.h2`
  font-family: "Cascadia Code PL";
`;

const SectionHeader = styled.h3``;

const RowHeader = styled.h4``;

export { TitleHeader, SectionHeader, RowHeader };
